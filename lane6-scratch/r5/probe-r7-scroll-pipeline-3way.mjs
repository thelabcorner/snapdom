#!/usr/bin/env node
// R7 BRST1 x BRST2 x BRST3 deterministic 2^3 native-getter factorial. NO wall time.
import fs from 'node:fs'
import http from 'node:http'
import path from 'node:path'
import { chromium, firefox, webkit } from 'playwright'
import { PAGE_FIXTURE_SRC, FIXTURE_OPTIONS } from '../atlas/profiler/fixtures.mjs'
const ROOT=process.cwd(), mod=fs.readFileSync(path.join(ROOT,'worktrees/snapdom-v3-r7-style-authority-integration/dist/snapdom.mjs'))
const PAGE=`<!doctype html><html><body><script>${PAGE_FIXTURE_SRC.replaceAll('</script>','<\\/script>')}</script><script type="module">window.__m=await import('/m.mjs');window.__ready=true</script></body></html>`
const server=http.createServer((req,res)=>{if(req.url?.startsWith('/m.mjs')){res.writeHead(200,{'content-type':'text/javascript','cache-control':'no-store'});res.end(mod);return}res.writeHead(200,{'content-type':'text/html','cache-control':'no-store'});res.end(PAGE)})
await new Promise(r=>server.listen(0,'127.0.0.1',r));const origin=`http://127.0.0.1:${server.address().port}`
const props=['scrollWidth','clientWidth','scrollHeight','clientHeight','scrollLeft','scrollTop']
async function arm(bt,fixture,b,w,c){const browser=await bt.launch({headless:true});const page=await browser.newPage({viewport:{width:1400,height:1800},deviceScaleFactor:1});try{await page.goto(origin);await page.waitForFunction(()=>window.__ready===true);return await page.evaluate(async({fixture,b,w,c,opts,props})=>{let root,cleanup;if(fixture==='entropy-400'){const st=document.createElement('style');st.textContent='.r{width:900px}.x{display:block;width:120px;white-space:nowrap}';document.head.appendChild(st);root=document.createElement('div');root.className='r';for(let i=0;i<400;i++){const e=document.createElement('div');e.className='x';e.textContent='row '+i+' xxxxxxxxxxxxxxxxxxxxx';root.appendChild(e)}document.body.appendChild(root);cleanup=()=>{root.remove();st.remove()}}else{root=window.__fx.build(fixture);cleanup=()=>window.__fx.cleanup(root)}const counts=Object.fromEntries(props.map(x=>[x,0])),restore=[];for(const prop of props){let owner=Element.prototype;while(owner&&!Object.getOwnPropertyDescriptor(owner,prop))owner=Object.getPrototypeOf(owner);const d=owner&&Object.getOwnPropertyDescriptor(owner,prop);if(!d?.get||!d.configurable)continue;Object.defineProperty(owner,prop,{...d,get(){counts[prop]++;return d.get.call(this)}});restore.push(()=>Object.defineProperty(owner,prop,d))}try{const raw=await window.__m.snapdom.toRaw(root,{...opts,burst:true,__burstSemanticScrollTracking:b,__wrapScrolledSemanticGate:w,__burstCaptureScrollBaseline:c});return{raw,counts}}finally{while(restore.length)restore.pop()();cleanup()}},{fixture,b,w,c,opts:FIXTURE_OPTIONS,props})}finally{await page.close();await browser.close()}}
try{for(const[engine,bt]of Object.entries({chromium,firefox,webkit})){console.log(`\n${engine}`);for(const fixture of ['cards400-safe','entropy-400']){const out={};for(const b of[false,true])for(const w of[false,true])for(const c of[false,true])out[`${+b}${+w}${+c}`]=await arm(bt,fixture,b,w,c);const ref=out['000'].raw;console.log(fixture,Object.values(out).every(x=>x.raw===ref)?'PARITY=PASS':'PARITY=FAIL');for(const key of Object.keys(out).sort()){const x=out[key].counts,d=x.scrollWidth+x.clientWidth+x.scrollHeight+x.clientHeight,o=x.scrollLeft+x.scrollTop;console.log(`  ${key} dims=${d} offsets=${o} total=${d+o}`)}}}}finally{await new Promise(r=>server.close(r))}
